spring:
    application:
        name: order
    data:
        mongodb:
            uri: ${MONGODB_URI}

order-api:
    base-url: ${ORDER_API_BASE_URL}

springdoc:
    api-docs:
        path: /api-docs
    swagger-ui:
        path: /swagger-ui.html
        operationsSorter: method